syntax = "proto3";

message Search {
    string query = 1;
    int32 page_number = 2;
    int32 result_per_page = 3;
}

message Response {
    bool result = 1;
}

service HelloService {
    rpc Hello (Search) returns (Response);
}